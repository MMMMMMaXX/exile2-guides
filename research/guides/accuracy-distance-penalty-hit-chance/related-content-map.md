# Related Content Map — accuracy-distance-penalty-hit-chance

Cross-guide references are plain-text mentions only (guides have no relatedGuideIds field, per spec §0/§3).

Articles referenced by name in body prose / suggestions:

- Critical Hit guide — for the crit interaction section (a miss cannot crit).
- Projectile Behaviour guide — for ranged/distance penalty context.
- Builds guides — for investing Accuracy via gear and passives.
- Skills guides — for which skills check Accuracy vs bypass it.

No relatedBuildIds / relatedBossIds / relatedItemIds / relatedPatchIds / relatedSkillIds are populated (all empty arrays, per spec).
