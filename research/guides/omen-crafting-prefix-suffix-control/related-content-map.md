# Related Content Map — omen-crafting-prefix-suffix-control

Plain-text cross-guide mentions used inside the article body (no `relatedGuideIds` field exists in schema).

- **item-modifiers** — referenced in `preparation` and `quick-answer` as the prerequisite read for understanding prefixes/suffixes and slot counts. Linked by name in prose.
- **essence-crafting** — referenced in `before-after-item` and FAQ for forcing specific modifier tiers (essences guarantee a specific mod; Omens only control the side). Linked by name in prose.

Both are mentioned in natural language within sections; no structured relation arrays are populated (they remain empty `[]` per SPEC).

No other guides are linked. If those guides exist in the repo, the prose mentions give editors a plain-text hook to wire up later.
