# Conflict Log

Conflicting community info encountered and how it was resolved.

1. **Mac path** — Some community posts imply a macOS BuildPlanner folder exists; official docs only document Windows/SteamOS (Proton) paths. _Resolved_: Mac row marked unverified (`checkedAt 2026-08-02`), no invented path.
2. **Console support** — One source was ambiguous; games.gg explicitly states consoles lack `.build` codes as of 0.5.0. _Resolved_: state no console support, cite 0.5.0.
3. **"Run the file" wording** — A few guides loosely say "run the build". _Resolved_: consistently describe it as data placed in a folder, never executed.
4. **DPS claim** — Forums sometimes call it a "planner/sim". _Resolved_: explicitly state it is not a DPS simulator per developer docs.
