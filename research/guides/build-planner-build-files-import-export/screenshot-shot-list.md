# Screenshot Shot List

In-game UI shots that would strengthen this guide (descriptive only — not generated here).

1. **File Explorer BuildPlanner folder** — shows the exact Windows directory with a `.build` file inside.
2. **Passive Skill Tree with blue overlay** — the guided allocation line after selecting a build.
3. **Blue icon / dropdown top-left** — the build selector menu.
4. **Gemcutting tab** — recommended skill/support gems listed for the build.
5. **Gear slot hover** — the blue icon tooltip showing stat-priority gear notes.
6. **Weapon-set colour lines** — red/green lines indicating set I / II links.
7. **Multiple variant files** — a folder with several Level/Budget `.build` files for one build.
8. **Export dialog** — the in-game "Export Build Planner" button producing a `.build`.
