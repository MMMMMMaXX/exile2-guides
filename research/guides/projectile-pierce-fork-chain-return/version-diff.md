# Version Diff — projectile-pierce-fork-chain-return

## PoE1 vs PoE2 notes to AVOID copying
- This is a PoE2 site. Do not copy PoE1 behaviour as if current.
- In PoE2 the global collision priority (Split > Pierce > Fork > Chain > Return) and the single-behaviour-per-collision rule are the documented model.

## 0.2.0 change noted
- Lightning Arrow was fixed so it cannot chain at all, including off terrain (previously a bug let it chain off terrain). Recorded as a real historical change; current behaviour is "cannot chain".

## Baseline patch
- Article baseline: Path of Exile 2 Early Access 0.5.4. Exact support gem counts/damage are version-sensitive and should be read from the in-game tooltip; no numbers invented.
