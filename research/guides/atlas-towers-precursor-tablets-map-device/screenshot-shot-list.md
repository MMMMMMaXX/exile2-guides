# Screenshot Shot List

In-game UI captures that would strengthen this guide (descriptive only — not generated here).

1. Atlas screen showing a Tower node icon among reachable maps (fog edges).
2. Inside the Tower map, the clear/completion moment (beacon activation prompt).
3. Atlas screen of a completed Tower with its Precursor Beacon active and 3 tablet slots open.
4. Placing a Precursor Tablet into a Tower slot from the Atlas screen (not inventory).
5. Tower radius ring overlay highlighting the maps it affects.
6. A Precursor Tablet tooltip showing its remaining-uses counter.
7. The Map Device interface with a Waystone inserted for a radius map.
8. A radius map in-progress showing the tablet's applied modifier banner.
9. Comparison overlay: legacy "right-click tower" vs current Atlas-screen placement.
