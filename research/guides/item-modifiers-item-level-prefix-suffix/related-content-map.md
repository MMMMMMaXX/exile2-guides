# Related Content Map — item-modifiers-item-level-prefix-suffix

Plain-text cross-guide mentions (no href per SPEC; mentioned by name only).

## Guides this article builds toward (named in closing overview)
- **Essence guide** — builds on this modifier framework (Essences force specific modifiers while upgrading rarity).
- **Omen guide** — builds on this framework (Omens make standard currencies more deterministic, e.g. choose prefix vs suffix).

## Referenced tooling (named, not linked as guides)
- PoE2DB and Craft of Exile — recommended by Maxroll for checking mod pools, iLvl tier requirements, and weights.

## Internal repo linkage
- This guide's `relatedBuildIds` / `relatedBossIds` / `relatedItemIds` / `relatedPatchIds` / `relatedSkillIds` are intentionally empty (no related slugs supplied).
- Cross-guide references are plain-text mentions inside overview/section prose only.

No published in-repo guides were linked via related*Ids because none were provided in the brief.
