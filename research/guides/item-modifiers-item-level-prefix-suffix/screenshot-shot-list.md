# Screenshot Shot List — item-modifiers-item-level-prefix-suffix

Descriptive list of in-game UI shots that would strengthen the article. NOT generated here.

1. **Default vs advanced tooltip** — same rare item shown with Alt released (tiers hidden) and Alt held (tiers, local/global, iLvl revealed). Demonstrates the core "read the advanced view" point.
2. **Prefix/suffix slot indicator** — a Magic item (1+1) and a Rare item (3+3) side by side showing affix group labels.
3. **Item Level vs Required Level** — tooltip of an item where iLvl and req level differ, annotated.
4. **Local vs global example** — weapon with local %physical (blue text) vs a global %increased damage modifier.
5. **"No available modifier" prompt** — currency used on a full/incompatible item showing the message.
6. **Modifier tier band** — advanced view highlighting a T1 modifier vs a lower-tier one on comparable bases.
7. **Base-type pool limit** — a bow and an armour tooltip showing non-overlapping mod pools.

All shots should be captured in Path of Exile 2 Early Access 0.5.4 client for version accuracy.
