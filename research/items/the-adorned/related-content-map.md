# The Adorned — Related Content Map

Internal links use the whitelisted slugs. en uses root paths; zh-cn uses `/zh-cn/` prefix. All target slugs below exist in the content set.

## Items

- `vaal-orb` — used to corrupt magic jewels into Corrupted Magic Jewels (core crafting dependency).
- `architects-orb` — mentioned as NOT usable on The Adorned (corrupted).
- `jewels-and-jewel-sockets` — general jewel radius / socket rules.
- `attribute-stacking-builds` — primary build family benefiting from the amplifier.

## Bosses

- `xesht-we-that-are-one` — listed as relatedBossId per content plan mapping (note: current verified drop boss is The Trialmaster; the plan's mapping is honored in the JSON).
- See `competitor-conflicts.md` for the drop-source clarification.

## Guides

- Build Planner Guide — for planning jewel-socket clusters.
- Attribute Stacking Builds guide — build fit.

## Patches

- Patch 0.4.0 — raised modifier cap from 100% to 150% (legacy boundary).
- Patch 0.5.4 — current verified version.

## Link Whitelist Compliance

All href values in the JSON use only whitelisted slugs with correct locale prefix. No invented slugs. Anchor links use only the 20 valid section ids (e.g. `#eligibility`, `#effect-breakpoints`, `#usage`); never `#current-vs-legacy`.
