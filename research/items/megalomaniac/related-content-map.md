# Megalomaniac — Related Content Map

Internal links use the whitelisted slugs. en uses root paths; zh-cn uses `/zh-cn/` prefix. All target slugs below exist in the content set.

## Items

- `simulacrum` — the encounter where Megalomaniac drops.
- `delirium` — the mechanic/league context for Simulacrum bosses.
- `from-nothing` — sibling notable-allocator unique (comparison).
- `the-adorned` — sibling Diamond unique (different role; cannot co-equip conceptually).

## Bosses

- `trialmaster` — listed as relatedBossId per content plan mapping (note: current verified drop is Simulacrum Delirium bosses; the plan's mapping is honored in the JSON; see `competitor-conflicts.md`).

## Guides

- Build Planner Guide — for planning where freed points go.
- Simulacrum / Delirium guides — farming strategy.

## Patches

- Patch 0.5.0 — Megalomaniac began dropping identified (legacy boundary).
- Patch 0.5.4 — current verified version.

## Link Whitelist Compliance

All href values in the JSON use only whitelisted slugs with correct locale prefix. No invented slugs. Anchor links use only the 20 valid section ids (e.g. `#eligibility`, `#notable-classification`, `#usage`); never `#current-vs-legacy`.
